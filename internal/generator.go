package internal

import (
	"github.com/invopop/jsonschema"
)

// Generate takes the json schemas defined at the provided directory
// and attempts to create a set of markdown documents.
func Generate(path string) error {

	// Open the directory at the provided path

	// Go through each JSON Schema document

	// Parse as our jsonschema Schema model

	// Output as markdown

	return nil
}

func processSchema(schema *jsonschema.Schema) error {

	return nil
}
